/**
 * @deprecated This package is deprecated.
 * Use `import { getLogger } from '@ui/lib/logging'` for logging.
 * Use `import { isBrowser, loggerStyles } from '@ui/lib/logger'` for other utilities.
 */

/**
 * @deprecated Use `import { getLogger } from '@ui/lib/logging'` directly
 */
export { getLogger } from '@hive/ui/lib/logging';

/**
 * @deprecated Use `import { isBrowser, loggerStyles } from '@ui/lib/logger'` directly
 */
export { isBrowser, loggerStyles } from '@hive/ui/lib/logger';