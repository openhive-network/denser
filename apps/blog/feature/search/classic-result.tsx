import { SearchSort } from '@ui/hooks/useSearch';

const ClassicResult = ({ query, sort }: { query: string; sort: SearchSort }) => {
  return (
    <div>
      <h2>Classic Search</h2>
      <p>Not supported yet</p>
    </div>
  );
};

export default ClassicResult;
