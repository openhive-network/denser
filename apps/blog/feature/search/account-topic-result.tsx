import { SearchSort } from '@ui/hooks/useSearch';

const AccountTopicResult = ({
  account,
  query,
  sort
}: {
  account: string;
  query: string;
  sort: SearchSort;
}) => {
  return (
    <div>
      <h2>Account Topic Search</h2>
      <p>Not supported yet</p>
    </div>
  );
};

export default AccountTopicResult;
